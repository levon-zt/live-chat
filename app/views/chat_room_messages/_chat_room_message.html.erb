<% if chat_room_id == chat_room_message.chat_room_user.chat_room_id %>
            <%= puts is_user_message_owner %>

  <%= turbo_frame_tag dom_id(chat_room_message) do %>
    <div class="mb-2">
      <div class="d-flex justify-content-<%= is_user_message_owner ? 'end' : 'start' %>">
        <div class="fs-12"><%= chat_room_message.created_at.strftime("%I:%M %p") %></div>
      </div>
      <div class="d-flex justify-content-<%= is_user_message_owner ? 'end' : 'start' %>">
        <div class="bg-white p-2 border rounded-<%= is_user_message_owner ? 'start' : 'end' %>"><%= chat_room_message.message %></div>
      </div>
    </div>
  <% end %>
<% end %>